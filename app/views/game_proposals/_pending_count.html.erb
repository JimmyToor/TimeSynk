<%# locals: (count:0) %>
<span id="pending_proposal_count_label" class="sr-only">View game proposals <%= count %> pending.</span>
<%= content_tag :span,
                'aria-hidden': true,
                class:"inline-flex items-center justify-center w-3 h-3 p-3 ms-3 text-sm font-medium text-blue-800
                 bg-blue-100 rounded-full dark:bg-blue-900 dark:text-blue-300 empty:hidden" do %>
  <%= count -%>
<% end %>