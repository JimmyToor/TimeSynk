<%# locals: (schedules:) %>
<div class="overflow-x-auto">
  <table class="w-full text-sm text-left text-gray-500 dark:text-gray-400">
    <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
      <tr>
        <th></th>
        <th scope="col" class="px-4 py-3">Schedule</th>
        <th scope="col" class="px-4 py-3 hidden md:table-cell text-center">Description</th>
        <th scope="col" class="px-4 py-3 hidden lg:table-cell text-center">Frequency</th>
        <th scope="col" class="px-4 py-3 hidden xl:table-cell text-center">Start Date</th>
        <th scope="col" class="px-4 py-3 hidden xl:table-cell text-center">End Date</th>
        <th scope="col" class="px-4 py-3 hidden 2xl:table-cell text-center">Duration</th>
      </tr>
    </thead>
    <tbody data-controller="element-toggle" data-element-toggle-display-style-value="table-row">
      <% schedules.each do |schedule| %>
        <%= render partial:"schedule_table_row", locals: {schedule: schedule} %>
      <% end %>
    </tbody>
  </table>
</div>
<%= render partial: "shared/table_nav" %>