<%= turbo_stream.update("modal_frame", "") %>

<%= turbo_stream.remove("new_ping") %>

<%= turbo_stream.append("show_availability_schedule_list",
                        "<li class='inset-2'> #{@schedule.name} </li>")  %>

<%= turbo_stream_toast(:success, I18n.t("schedule.create.success"), "schedule_#{@schedule.id}") %>

<% @schedule.availabilities.each do |availability| %>
  <%= frame_reload "availability_#{availability.id}" %>
<% end %>

<%= frame_reload "schedule_table" %>