<%= turbo_stream_toast(:success,
                       I18n.t("invite.update.success"),
                       "invite_#{@invite.id}") %>

<%= turbo_stream.update("modal_frame") %>

<%= turbo_stream.update("invite_table_row_#{@invite.id}",
                        partial: "invites/table/invite_table_row",
                        locals: { invite: @invite}) %>