<%# locals: (title: "Open Deletion Dialog", data: {}, extra_classes: "") %>

<%= button_tag data: {action: "dialog#open"}.merge(data),
  title: title,
  class: "text-notice-600 dark:text-notice-400 #{extra_classes}" do %>
  <%= content_tag(:span, inline_svg("icons/trash.svg")) %>
<% end %>