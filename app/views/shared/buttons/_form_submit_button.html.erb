<%# locals: (form:, text: "Submit", name: "commit", data: {}, extra_classes: "") %>

<%= form.submit text,
  name: name,
  data: data,
  class: "text-black cursor-pointer dark:text-white focus:ring-4  rounded-lg text-sm px-5 py-2.5 font-semibold
    #{visit_link_bg_classes} #{extra_classes}" %>