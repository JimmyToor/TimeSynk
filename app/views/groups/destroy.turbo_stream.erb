<%= turbo_stream_toast(:success,
                       I18n.t("group.destroy.success", name: @group.name),
                       "group_#{@group.id}",
                       highlight: " #{@group.name} ") %>

<%= turbo_stream.update "modal_frame" %>

<%= turbo_stream.remove @group %>

<%= turbo_stream.remove "#{@group.id}_container" %>