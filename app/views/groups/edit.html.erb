<h1>Editing group</h1>
<%= modal_wrapper(local_assigns[:original_name] || @group.name) do %>
  <%= render "form", group: @group %>

  <br>

  <div>
    <%= link_to "Cancel", "#", data: {action: "click->dialog#close", turbo: "false"} %>
  </div>
<% end %>

<%= link_to "Back to groups", groups_path %>
