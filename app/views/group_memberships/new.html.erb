<%# locals: (invite: nil) %>
<%= render "shared/notification", id: "new_group_membership" %>

<% if invite.present? && !invite.errors.any? %>
  <%= render "accept_invite", invite: invite  %>
<% else %>
  <%= render "form" %>
<% end %>
