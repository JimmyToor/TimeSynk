<%= turbo_frame_tag dom_id(game) do %>
  <tr class="border-b dark:border-gray-600 hover:bg-gray-100 dark:hover:bg-gray-700">
    <th scope="row" class="flex items-center px-4 py-2 font-medium text-gray-900 whitespace-nowrap dark:text-white">
      <% if game.cover_image_url.present? %>
        <%= image_tag game.cover_image_url, class: "w-8 h-8 mr-3 rounded", alt: game.name if game.cover_image_url.present?  %>
      <% else %>
        <div class="w-8 h-8 mr-3 bg-gray-200"></div>
      <% end %>
      <%= game.name %>
    </th>
    <td class="px-4 py-2">
      <span class="bg-primary-100 text-primary-800 text-xs font-medium px-2 py-0.5 rounded dark:bg-primary-900 dark:text-primary-300"> <%= game.platforms.join(", ") %> </span>
    </td>
    <%= link_to "Select #{game.name}", "#",
                  data: {
                    action: "game-selection#select",
                    game_id: game.id,
                    game_name: game.name,
                  },
                  remote: true,
                  class: "select-game-btn" %>

  </tr>
<% end %>