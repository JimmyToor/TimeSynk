<!DOCTYPE html>
<html>
  <head>
    <title>timeSynk</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "recurring_select", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "recurring_select", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="dark:bg-gray-900 dark:text-white">
    <%= render "shared/navbar" %>

    <div class="p-4 sm:ml-64">
      <div class="p-4 mt-14">
        <%= yield %>
      </div>
    </div>

    <%= render 'shared/modal' %>
  </body>
</html>
